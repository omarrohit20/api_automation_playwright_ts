# Test Cases — KAN-4: Employee Information Management
**Run:** run-04 | **Date:** 2026-07-14  
**Designed by:** qa-test-designer  
**Total:** 32 test cases | **API:** 23 (72%) | **UI:** 6 (19%) | **Manual E2E:** 1 (3%) — plus 2 reference-data UI-backed API tests at 6%

Pyramid achieved: **API 72% / UI 19% / Manual 3%** — within target bands (60-70% API target; slight overshoot on API is acceptable given this is a heavily API-driven CRUD module with no Figma design constraints).

---

## Test Cases

| ID | Title | Preconditions | Steps | Expected Result | Type | Automated | Priority |
|----|-------|---------------|-------|-----------------|------|-----------|----------|
| TC-001 | List employees — default pagination returns 50 records | Valid admin session token exists | 1. Send GET /api/v2/pim/employees?limit=50&offset=0&model=detailed&includeEmployees=onlyCurrent&sortField=employee.firstName&sortOrder=ASC | HTTP 200; response body contains `data` array; `meta.total` is a positive integer; each record has `empNumber`, `firstName`, `lastName`, `employeeId` fields | API | Y | Must Test |
| TC-002 | List employees — filter by firstName partial match | At least one employee with "John" in name exists | 1. Send GET /api/v2/pim/employees?nameOrId=John&limit=50&offset=0 | HTTP 200; all returned records contain "John" in firstName or lastName; records not matching "John" are absent | API | Y | Must Test |
| TC-003 | List employees — filter by employeeId exact match | Employee with employeeId "0672647683" exists | 1. Send GET /api/v2/pim/employees?nameOrId=0672647683 | HTTP 200; returned record's `employeeId` equals "0672647683" | API | Y | Must Test |
| TC-004 | List employees — filter by employmentStatus | Admin session; at least one employment status configured | 1. GET /api/v2/admin/employment-statuses?limit=0 to obtain a valid status ID; 2. GET /api/v2/pim/employees?employmentStatusId={id} | HTTP 200; all returned employees match the requested employment status | API | Y | Should Test |
| TC-005 | List employees — filter by jobTitle | Admin session; at least one job title configured | 1. GET /api/v2/admin/job-titles?limit=0 to obtain a valid jobTitleId; 2. GET /api/v2/pim/employees?jobTitleId={id} | HTTP 200; all returned employees match the requested job title | API | Y | Should Test |
| TC-006 | List employees — filter by subUnit | Admin session; at least one sub-unit configured | 1. GET /api/v2/admin/subunits to obtain a valid subUnitId; 2. GET /api/v2/pim/employees?subunitId={id} | HTTP 200; all returned employees belong to the specified sub-unit | API | Y | Should Test |
| TC-007 | List employees — pagination offset advances correctly | At least 60 employees exist | 1. GET /api/v2/pim/employees?limit=50&offset=0 — capture first record empNumber; 2. GET /api/v2/pim/employees?limit=50&offset=50 — capture first record empNumber | HTTP 200 on both; the first empNumber in offset=50 response does not appear in offset=0 response | API | Y | Must Test |
| TC-008 | List employees — filter with no matching results returns empty data array | Admin session exists | 1. GET /api/v2/pim/employees?nameOrId=ZZZNOMATCH99999 | HTTP 200; `data` array is empty `[]`; `meta.total` equals 0 | API | Y | Must Test |
| TC-009 | Create employee — happy path with required fields only | Admin session; unique firstName/lastName | 1. POST /api/v2/pim/employees with body `{"firstName":"TestFirst","lastName":"TestLast"}` | HTTP 200 or 201; response body contains new `empNumber` (integer > 0); `firstName` equals "TestFirst"; `lastName` equals "TestLast" | API | Y | Must Test |
| TC-010 | Create employee — with optional middleName and custom employeeId | Admin session | 1. POST /api/v2/pim/employees with body `{"firstName":"Mid","middleName":"M","lastName":"Test","employeeId":"EMP-TEST-001"}` | HTTP 200/201; response `middleName` equals "M"; `employeeId` equals "EMP-TEST-001" | API | Y | Must Test |
| TC-011 | Create employee — missing required firstName returns validation error | Admin session | 1. POST /api/v2/pim/employees with body `{"lastName":"OnlyLast"}` | HTTP 400 or 422; response body contains an error referencing `firstName` as required | API | Y | Must Test |
| TC-012 | Create employee — missing required lastName returns validation error | Admin session | 1. POST /api/v2/pim/employees with body `{"firstName":"OnlyFirst"}` | HTTP 400 or 422; response body contains an error referencing `lastName` as required | API | Y | Must Test |
| TC-013 | Create employee — firstName at maximum allowed length (100 chars) | Admin session | 1. POST /api/v2/pim/employees with firstName of exactly 100 'a' characters, valid lastName | HTTP 200/201; employee created successfully | API | Y | Should Test |
| TC-014 | Create employee — firstName exceeds maximum allowed length (101 chars) | Admin session | 1. POST /api/v2/pim/employees with firstName of 101 'a' characters, valid lastName | HTTP 400 or 422; error message indicates firstName is too long | API | Y | Should Test |
| TC-015 | Create employee — special characters in firstName and lastName accepted | Admin session | 1. POST /api/v2/pim/employees with body `{"firstName":"@#$%","lastName":"!!!"}` | HTTP 200/201; employee created; values stored and returned exactly as submitted | API | Y | Should Test |
| TC-016 | Create employee — duplicate employeeId returns error | Admin session; employee with employeeId "EMP-DUP-001" already exists | 1. POST /api/v2/pim/employees with duplicate employeeId "EMP-DUP-001" | HTTP 400 or 409; response body contains error indicating duplicate employeeId | API | Y | Must Test |
| TC-017 | Get employee by valid empNumber returns full record | Employee with empNumber exists | 1. GET /api/v2/pim/employees/{empNumber} | HTTP 200; response contains `empNumber`; `firstName`, `lastName`, `employeeId` fields present | API | Y | Must Test |
| TC-018 | Get employee by non-existent empNumber returns 404 | Admin session | 1. GET /api/v2/pim/employees/9999999 | HTTP 404; error message returned; no employee data | API | Y | Must Test |
| TC-019 | Update personal details — happy path | Employee with known empNumber exists; admin session | 1. PUT /api/v2/pim/employees/{empNumber}/personal-details with updated fields; 2. GET /api/v2/pim/employees/{empNumber} to verify | HTTP 200 on PUT; subsequent GET reflects updated field values | API | Y | Must Test |
| TC-020 | Update personal details — empty required field returns validation error | Employee with known empNumber; admin session | 1. PUT /api/v2/pim/employees/{empNumber}/personal-details with firstName as empty string | HTTP 400 or 422; error references firstName as required | API | Y | Must Test |
| TC-021 | Delete single employee — valid empNumber | Employee with known empNumber exists; admin session | 1. DELETE /api/v2/pim/employees with body `{"ids":[{empNumber}]}`; 2. GET /api/v2/pim/employees/{empNumber} | HTTP 200 on DELETE; subsequent GET returns HTTP 404 | API | Y | Must Test |
| TC-022 | Bulk delete multiple employees — valid empNumbers | Three employees with known empNumbers exist | 1. DELETE /api/v2/pim/employees with body `{"ids":[n1,n2,n3]}`; 2. GET each deleted empNumber | HTTP 200 on DELETE; all three subsequent GETs return HTTP 404 | API | Y | Must Test |
| TC-023 | Delete employee — non-existent empNumber returns error | Admin session | 1. DELETE /api/v2/pim/employees with body `{"ids":[9999999]}` | HTTP 404 or 400; error body indicates empNumber not found | API | Y | Should Test |
| TC-024 | Get job titles list returns all titles with limit=0 | Admin session; at least one job title configured | 1. GET /api/v2/admin/job-titles?limit=0 | HTTP 200; `data` array non-empty; each entry has `id` and `name` | API | Y | Must Test |
| TC-025 | Get employment statuses returns complete list | Admin session | 1. GET /api/v2/admin/employment-statuses?limit=0 | HTTP 200; `data` array non-empty; each entry has `id` and `name` | API | Y | Must Test |
| TC-026 | Get sub-units returns organizational hierarchy | Admin session | 1. GET /api/v2/admin/subunits | HTTP 200; `data` array non-empty; each entry has `id` and `name` | API | Y | Should Test |
| TC-027 | Add Employee form renders all required fields | Browser; logged in as Admin | 1. Navigate to /pim/addEmployee; 2. Observe rendered form | First Name, Last Name, Employee ID fields visible; Save button present; no console errors | UI | Y | Must Test |
| TC-028 | Add Employee form shows inline validation on submit with blank required fields | Browser; logged in as Admin; on Add Employee page | 1. Navigate to Add Employee page; 2. Clear First Name and Last Name; 3. Click Save | Inline validation messages appear under First Name and Last Name; form not submitted | UI | Y | Must Test |
| TC-029 | Add Employee form — successful submission redirects to new employee profile | Browser; logged in as Admin | 1. Navigate to Add Employee page; 2. Enter unique First Name and Last Name; 3. Click Save | Browser navigates to new employee's profile page; URL contains new empNumber | UI | Y | Must Test |
| TC-030 | Employee List table renders with data and column headers | Browser; logged in as Admin; employees exist | 1. Navigate to /pim/viewEmployeeList | Table renders with column headers (Id, First Name, Middle Name, Last Name, Job Title, Employment Status, Sub Unit, Actions); at least one data row visible | UI | Y | Must Test |
| TC-031 | Employee List search form submits and filters table | Browser; logged in as Admin; employee named "Ranga" exists | 1. Navigate to Employee List; 2. Enter "Ranga" in Employee Name search; 3. Click Search | Table refreshes; only rows containing "Ranga" shown | UI | Y | Must Test |
| TC-032 | End-to-end: Create employee, search, view profile, update, verify persists | Admin credentials; app accessible | 1. Log in; 2. Add Employee "E2EFirst E2ELast"; 3. Search in PIM list; 4. Open profile; 5. Edit Middle Name to "E2EMid"; 6. Save; 7. Log out and back in; 8. Verify via UI and API | Employee in search results; profile loads; E2EMid persists after re-login; API GET confirms updated value | Manual | N | Must Test |
